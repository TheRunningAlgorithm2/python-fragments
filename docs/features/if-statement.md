# If-statement

Conditionally render elements using any valid Python conditional.

```python
def my_component(...):
    return <>
        <div if={{ user.is_admin }}>
            ...
        </div>
    </>
```
