# For-loop

Loop over any iterable exactly the same as you would in vanilla Python.

```python
def my_component():
    a = [1, 2, 3, 4, 5]
    return <>
        <ul>
            <li for={{ value in a }}>
                {{ value }}
            </li>
        </ul>
    </>
```
